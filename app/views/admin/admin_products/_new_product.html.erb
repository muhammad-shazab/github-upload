           <div class="container">  
             <%= form_for([:admin,@pro],url: admin_admin_products_path) do |form| %>
                  <% if @pro.errors.any? %>
                    <div id="error_explanation">
                      <h2>
                        <%= pluralize(@pro.errors.count, "error") %> prohibited
                        this book from being saved:
                      </h2>
                      <ul>
                        <% @pro.errors.full_messages.each do |msg| %>
                          <li><%= msg %></li>
                        <% end %>
                      </ul>
                    </div>
                  <% end %>

                <div class="form-group">
                      <%= form.label :category, 'Category' %>
                      <%= form.text_field :category,  {class: 'form-control',id: 'category-field',required: true}%>
                </div>
                 <div class="modal-footer">
                  <%= form.submit "Add Product Category",class: "btn btn-primary"%>
                  <button class="btn btn-secondary" data-dismiss="modal">Close</button>
                </div>
              <%end%>
            </div>  
                
