<div class="container">  
                   <%= form_for(@user) do |form| %>
                        <% if @product.errors.any? %>
                          <div id="error_explanation">
                            <h2>
                              <%= pluralize(@product.errors.count, "error") %> prohibited
                              this book from being saved:
                            </h2>
                            <ul>
                              <% @product.errors.full_messages.each do |msg| %>
                                <li><%= msg %></li>
                              <% end %>
                            </ul>
                          </div>
                        <% end %>
                       <div class="modal-footer">
                        <%= form.submit "Add Products",class: "btn btn-primary"%>
                        <button class="btn btn-secondary" data-dismiss="modal">Close</button>
                      </div>
                    <%end%>
                </div> 


                <div class="alert alert-dismissible alert-success">
  <button type="button" class="close" data-dismiss="alert">&times;</button>
  <strong>Well done!</strong> You successfully read <a href="#" class="alert-link">this important alert message</a>.
</div>



<div class="ml-auto" style="float: right;padding-top: 100px" id="switch">
          <%=link_to 'switch to user view',users_path,:style=>"font-style:italic ;color:white;text-decoration:none"%>
        </div>